name: Drift bottle
description: Leave one line. It floats on the profile until the next bottle washes ashore.
title: Drift bottle
labels: [bottle]
body:
  - type: markdown
    attributes:
      value: |
        Only the **first line** is shown, up to 48 characters, alongside your username.
  - type: textarea
    id: message
    attributes:
      label: Your one line
      placeholder: Write something…
    validations:
      required: true
